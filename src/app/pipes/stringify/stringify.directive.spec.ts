import { StringifyDirective } from './stringify.directive';

describe('StringifyDirective', () => {
  it('should create an instance', () => {
    const directive = new StringifyDirective();
    expect(directive).toBeTruthy();
  });
});
